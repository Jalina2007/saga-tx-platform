spring:
  application:
    name: coordinator-service
  datasource:
    url: jdbc:postgresql://localhost:5432/coordinator_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
